var google = require('googleapis');
var Q = require('q');


module.exports = {
  createHunt: function(name, active) {
    // TODO
    var defer = Q.defer();
    defer.resolve(null);
    return defer.promise;
  }

};
